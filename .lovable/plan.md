

# Remover logo do painel esquerdo

Remover apenas a tag `<img>` da logo completa (linhas 93-97) do painel esquerdo (sobre a foto). Manter o texto/tagline e toda a estrutura. O painel direito com a logo icone no login permanece inalterado.

**Arquivo:** `src/pages/Index.tsx`
- Deletar linhas 93-97 (a tag `<img src={logoFull} .../>`)

